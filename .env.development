# Local development only (`npm run dev`). Overrides `.env` for the same keys.
# Matches infrastructure/docker-compose.yml (Keycloak published on host 8090).

VITE_APP_URL=http://localhost:5173
VITE_PORT=5173
VITE_KEYCLOAK_URL=http://localhost:8090
VITE_API_BASE=http://localhost:5000

