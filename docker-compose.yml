version: "3.9"

services:
  backend:
    build:
      context: ./backend
    container_name: app_backend
    ports:
      - "8000:5000"
    env_file:
      - ./backend/.env
    volumes:
      - ./backend:/app
    restart: unless-stopped

