# Optional: copy to `.env.local` in the repo root (gitignored).
# Vite loads `.env.local` in every mode, including `vite build`, so only put
# values here if they are safe for production builds — otherwise use
# `.env.development` for dev-only overrides.
